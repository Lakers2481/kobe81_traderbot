# Kobe Trading System Dependencies
# Using minimum version specifiers for compatibility

# Core Data Processing
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# HTTP & API
requests>=2.31
aiohttp>=3.9
websockets>=12.0

# Broker Integration
alpaca-py>=0.13

# Configuration
pyyaml>=6.0
python-dotenv>=1.0

# Free data fallbacks (used by multi_source provider)
yfinance>=0.2.28
pandas-datareader>=0.10

# Technical Analysis
ta>=0.11.0
pandas-ta-classic # For technical indicators on Python 3.11
# pandas-ta requires Python 3.12+, install manually if needed:

# ML & AI Features
stumpy>=1.12  # Matrix profile for anomaly detection
scikit-learn>=1.3  # ML algorithms and preprocessing
scipy>=1.11  # Scientific computing
joblib>=1.3  # model serialization
vaderSentiment>=3.3  # sentiment analysis (VADER)
anthropic>=0.21  # For Claude API integration

# Visualization (for reports)
matplotlib>=3.7
plotly>=5.18

# Testing
pytest>=7.4
pytest-cov>=4.1
pytest-asyncio>=0.21
pytest-mock>=3.12
requests-mock>=1.11

# Type Checking
mypy>=1.7

# Code Quality
black>=23.0
flake8>=6.1
isort>=5.12

# Scheduling
schedule>=1.2

# Date/Time
python-dateutil>=2.8
pytz>=2023.3

# HTTP Server (for health endpoints)
flask>=3.0

# Dashboard (Mission Control)
fastapi>=0.104
uvicorn>=0.24

# Alerts & Notifications
python-telegram-bot>=20.0

# Bayesian Optimization
optuna>=3.4

# Market Regime Detection (HMM)
hmmlearn>=0.3

# Ensemble ML (optional - for advanced ML features)
# Install these manually for full ensemble functionality:
# pip install xgboost>=2.0
# pip install lightgbm>=4.0

# =============================================================================
# NEW RESEARCH-BASED ENHANCEMENTS (2026-01-01)
# Based on comprehensive research across GitHub, academic papers, and quant libraries
# =============================================================================

# FinBERT Sentiment Analysis
transformers>=4.36.0
torch>=2.0  # Required for FinBERT, can use CPU-only version

# Portfolio Analytics & Reporting
quantstats>=0.0.62  # Modern tearsheet generation (replaces pyfolio)
empyrical>=0.5.5  # Risk metrics (alpha, beta, Sharpe)

# Portfolio Optimization
pyportfolioopt>=1.5.5  # Mean-variance, HRP, Black-Litterman
riskfolio-lib>=6.0.0  # CVaR, CDaR, factor models

# Time Series Features (Lopez de Prado)
# fracdiff>=0.9.0  # Fractional differentiation - requires Python <3.10, install manually if needed
# mlfinlab is commercial - use our custom triple_barrier.py instead

# Change Point Detection
ruptures>=1.1.8  # PELT, Dynp for regime detection

# Factor Analysis
alphalens-reloaded>=0.4.3  # Factor validation (Quantopian fork)

# ICT / Smart Money Concepts (pip install if using enhanced ICT)
# smartmoneyconcepts>=0.0.21  # Order blocks, FVG, liquidity sweeps

# Advanced Time Series Features
# tsfresh>=0.20.0  # 787 automated features (large dependency)

# Fast Backtesting (optional - significant speed improvement)
# vectorbt>=0.26.0  # 10-100x faster vectorized backtesting

# Temporal Fusion Transformer (advanced ML forecasting)
# pytorch-forecasting>=1.0.0  # TFT implementation
# pytorch-lightning>=2.0.0  # Training framework

# =============================================================================
# ML INTERPRETABILITY (2026-01-02)
# Based on comprehensive resource analysis
# =============================================================================
shap>=0.44.0  # SHAP for feature importance (TreeExplainer for XGBoost/LightGBM)
